export const ACHIEVEMENTS = {
  // Normal Achievements
  firstWin: {
    id: 'firstWin',
    name: 'First Victory',
    description: 'Win your first game',
    icon: 'trophy.png',
    unlocked: false,
    category: 'Normal'
  },
  perfectGame: {
    id: 'perfectGame',
    name: 'Perfect Game',
    description: 'Win a game with 9 points',
    icon: 'perfect.png',
    unlocked: false,
    category: 'Normal'
  },
  tripleKO: {
    id: 'tripleKO',
    name: 'Triple Knockout',
    description: 'Win a 4-player game',
    icon: 'ko.png',
    unlocked: false,
    category: 'Normal'
  },

  // Challenge Achievements
  nullified: {
    id: 'nullified',
    name: 'System Error',
    description: 'Play NULL',
    icon: 'null.png',
    unlocked: false,
    category: 'Challenges'
  },
  reflection: {
    id: 'reflection',
    name: 'Final Performance',
    description: 'Trigger Reflection\'s ultimate ability',
    icon: 'reflection.png',
    unlocked: false,
    category: 'Challenges'
  },
  assassin: {
    id: 'assassin',
    name: 'Target Eliminated',
    description: 'Transform Savior into Assassin',
    icon: 'assassin.png',
    unlocked: false,
    category: 'Challenges'
  },
  awareness: {
    id: 'awareness',
    name: 'Infinite Power',
    description: 'Play Awareness or transform into it',
    icon: 'awareness.png',
    unlocked: false,
    category: 'Challenges'
  },
  glitchKingdom: {
    id: 'glitchKingdom',
    name: 'Glitch Kingdom',
    description: 'Have NULL, Moonlight and Awareness all in play',
    icon: 'null.png',
    unlocked: false,
    category: 'Challenges'
  },
  thatComedian: {
    id: 'thatComedian',
    name: 'That Comedian...',
    description: 'Have Assassin and Kite in play together',
    icon: 'assassin.png',
    unlocked: false,
    category: 'Challenges'
  },
  perfectChaos: {
    id: 'perfectChaos',
    name: 'Perfect Chaos',
    description: 'Win with both Chaos and Maturity in play',
    icon: 'chaos.png',
    unlocked: false,
    category: 'Challenges'
  },
  sisterDeleted: {
    id: 'sisterDeleted', 
    name: 'The Sister Deleted From Existence',
    description: 'Have an enhanced Moonlight in your deck',
    icon: 'moonlight.png',
    unlocked: false,
    category: 'Challenges'
  },
  greenThumb: {
    id: 'greenThumb',
    name: 'The Gardener',
    description: 'Win a game with at least one Hollowgreen anomaly in play',
    icon: 'herb.png',
    unlocked: false,
    category: 'Challenges'
  },
  parasite: {
    id: 'parasite',
    name: 'The Parasitic Garden',
    description: 'Have all 5 Hollowgreen anomalies in your deck at once',
    icon: 'herb.png',
    unlocked: false,
    category: 'Challenges'
  },
  awoken: {
    id: 'awoken',
    name: 'You Should Not Have Done That',
    description: 'Transform Ciph into their awakened form',
    icon: 'ciph_awake.png',
    unlocked: false,
    category: 'Challenges'
  },
  shieldMaster: {
    id: 'shieldMaster',
    name: 'Impenetrable Defense',
    description: 'Win with a Hollowgreen anomaly that has 10+ shield',
    icon: 'blossom.png',
    unlocked: false,
    category: 'Challenges'
  },
  parasiteSlayer: {
    id: 'parasiteSlayer',
    name: 'Glitch Slayer',
    description: 'Defeat Luna in a game',
    icon: 'awareness.png',
    unlocked: false,
    category: 'Challenges'
  },
  parasiteHunter: {
    id: 'parasiteHunter', 
    name: 'Parasite Hunter',
    description: 'Face Luna five times in combat. Now, just wait around the title screen.',
    icon: 'luna.png',
    unlocked: false,
    category: 'Challenges'
  },

  // Fun Stuff Achievements
  friendshipPower: {
    id: 'friendshipPower',
    name: 'Power of Friendship',
    description: 'Win with both KC and Penelope in play',
    icon: 'kc.png',
    unlocked: false,
    category: 'Fun Stuff'
  },
  mothersLove: {
    id: 'mothersLove',
    name: 'A Mother\'s Love',
    description: 'Win with both Feathers and Penelope in play',
    icon: 'feathers.png',
    unlocked: false,
    category: 'Fun Stuff'
  },
  demonAngel: {
    id: 'demonAngel',
    name: 'Heaven and Hell',
    description: 'Win with both Fevaa and Binx in play',
    icon: 'fevaa.png',
    unlocked: false,
    category: 'Fun Stuff'
  },
  subjectGuardian: {
    id: 'subjectGuardian',
    name: 'Unlikely Friends',
    description: 'Win with both Subject 192 and Cyrus in play',
    icon: 'subject_192.png',
    unlocked: false,
    category: 'Fun Stuff'
  },
  gingerSalvo: {
    id: 'gingerSalvo',
    name: 'Partners in Crime',
    description: 'Win with both Ginger and Salvo in play',
    icon: 'ginger.png',
    unlocked: false,
    category: 'Fun Stuff'
  },
  gardenParty: {
    id: 'gardenParty',
    name: 'Garden Party',
    description: 'Win with Terra and any Hollowgreen anomaly in play',
    icon: 'terra.png',
    unlocked: false,
    category: 'Fun Stuff'
  },

  // New achievements
  yourWorldIsMirror: {
    id: 'yourWorldIsMirror',
    name: 'YOUR WORLD\'S A MIRROR OF YOURSELF',
    description: 'Have SuNex in your deck',
    icon: 'sunex.png',
    unlocked: false,
    category: 'Special Cases'
  },
  sanctuarySquad: {
    id: 'sanctuarySquad',
    name: 'The Sanctuary Squad',
    description: 'Have Subject 192, Vicki, and Fossil all in your deck',
    icon: 'vicki.png',
    unlocked: false,
    category: 'Special Cases'
  },
  fireAndIce: {
    id: 'fireAndIce',
    name: 'Fire and Ice',
    description: 'Have Frost and any fire-elemental card in play',
    icon: 'frost.png',
    unlocked: false,
    category: 'Soul Heart Sky'
  },
  worldsRichest: {
    id: 'worldsRichest',
    name: 'This Achievement Was Made For $50,000',
    description: 'Have Goldie in your deck',
    icon: 'goldie.png',
    unlocked: false,
    category: 'Soul Heart Sky'
  },
  showmanship: {
    id: 'showmanship',
    name: 'Pure Showmanship',
    description: 'Have Ringmaster double a card\'s power to 20 or higher',
    icon: 'ringmaster.png',
    unlocked: false,
    category: 'Soul Heart Sky'
  },
  lightInDarkness: {
    id: 'lightInDarkness',
    name: 'Light in Darkness',
    description: 'Win with Sole while at least 2 cards are defeated',
    icon: 'sole.png',
    unlocked: false,
    category: 'Soul Heart Sky'
  },
  futurePast: {
    id: 'futurePast',
    name: 'Future Meets Past',
    description: 'Have Blitz, Fevaa, and Binx all in play at once',
    icon: 'blitz.png',
    unlocked: false,
    category: 'Soul Heart Sky'
  },
  plantSecrets: {
    id: 'plantSecrets',
    name: 'The Garden\'s Secrets',
    description: 'Have Lavender and any Hollowgreen anomaly in play',
    icon: 'lavender.png',
    unlocked: false,
    category: 'Soul Heart Sky'
  },
  grumpyKindness: {
    id: 'grumpyKindness',
    name: 'Grumpy Kindness',
    description: 'Have Leopold reduce the power of 3 or more friendly cards',
    icon: 'leopold.png',
    unlocked: false,
    category: 'Soul Heart Sky'
  },
  hitboxHero: {
    id: 'hitboxHero',
    name: 'Golden Hero',
    description: 'Win with Hitbox without their power being reduced to 1',
    icon: 'hitbox.png',
    unlocked: false,
    category: 'Soul Heart Sky'
  }
};

export class AchievementManager {
  constructor() {
    this.achievements = { ...ACHIEVEMENTS };
    this.loadAchievements();
    this.renderAchievements();
    this.setupEventListeners();
  }

  loadAchievements() {
    const savedAchievements = localStorage.getItem('achievements');
    if (savedAchievements) {
      const parsed = JSON.parse(savedAchievements);
      Object.keys(parsed).forEach(id => {
        if (this.achievements[id]) {
          this.achievements[id].unlocked = parsed[id].unlocked;
        }
      });
    }
  }

  saveAchievements() {
    localStorage.setItem('achievements', JSON.stringify(this.achievements));
  }

  unlockAchievement(id) {
    if (this.achievements[id] && !this.achievements[id].unlocked) {
      this.achievements[id].unlocked = true;
      this.saveAchievements();
      this.showAchievementPopup(this.achievements[id]);
      this.renderAchievements();
    }
  }

  showAchievementPopup(achievement) {
    const popup = document.getElementById('achievement-popup');
    const icon = document.getElementById('achievement-icon');
    const title = document.getElementById('achievement-title');
    const description = document.getElementById('achievement-description');

    icon.src = achievement.icon;
    title.textContent = achievement.name;
    description.textContent = achievement.description;

    popup.classList.add('show');

    setTimeout(() => {
      popup.classList.remove('show');
      // Give time for fade out animation before resetting position
      setTimeout(() => {
        popup.style.bottom = '-100px';
      }, 500);
    }, 3000);
  }

  renderAchievements() {
    const container = document.getElementById('achievements-list');
    if (!container) return;

    container.innerHTML = '';
    
    // Group achievements by category
    const categories = ['Normal', 'Challenges', 'Fun Stuff', 'Special Cases', 'Soul Heart Sky'];
    
    categories.forEach(category => {
      const categoryDiv = document.createElement('div');
      categoryDiv.className = 'achievement-category';
      
      const categoryTitle = document.createElement('h3');
      categoryTitle.textContent = category;
      categoryDiv.appendChild(categoryTitle);
      
      const categoryAchievements = Object.values(this.achievements)
        .filter(achievement => achievement.category === category);
      
      categoryAchievements.forEach(achievement => {
        const elem = document.createElement('div');
        elem.className = `achievement-item ${achievement.unlocked ? 'unlocked' : ''}`;
        
        elem.innerHTML = `
          <div class="achievement-icon" style="background-image: url(${achievement.icon})"></div>
          <div class="achievement-info">
            <div class="achievement-name">${achievement.name}</div>
            <div class="achievement-desc">${achievement.description}</div>
          </div>
        `;
        
        categoryDiv.appendChild(elem);
      });
      
      container.appendChild(categoryDiv);
    });
  }

  setupEventListeners() {
    const viewAchievementsBtn = document.getElementById('view-achievements');
    const achievementsModal = document.getElementById('achievements-modal');
    const closeBtn = achievementsModal?.querySelector('.close');
    const resetBtn = document.createElement('button');
    resetBtn.id = 'reset-achievements';
    resetBtn.className = 'main-button';
    resetBtn.textContent = 'Reset Achievements';
    resetBtn.style.marginTop = '20px';

    // Add reset button to modal
    if (achievementsModal) {
      achievementsModal.querySelector('.modal-content').appendChild(resetBtn);
    }

    // Reset achievements handler
    resetBtn.addEventListener('click', () => {
      if (confirm('Are you sure you want to reset all achievements? This cannot be undone.')) {
        Object.keys(this.achievements).forEach(id => {
          this.achievements[id].unlocked = false;
        });
        this.saveAchievements();
        this.renderAchievements();
      }
    });

    if (viewAchievementsBtn && achievementsModal) {
      viewAchievementsBtn.addEventListener('click', () => {
        achievementsModal.style.display = 'block';
      });
    }

    if (closeBtn && achievementsModal) {
      closeBtn.addEventListener('click', () => {
        achievementsModal.style.display = 'none';
      });

      window.addEventListener('click', (event) => {
        if (event.target === achievementsModal) {
          achievementsModal.style.display = 'none';
        }
      });
    }
  }

  checkGameEndAchievements(game) {
    if (game.scores[0] > 0) {
      this.unlockAchievement('firstWin');
    }
    if (game.scores[0] === 9) {
      this.unlockAchievement('perfectGame');
    }
    if (game.scores[0] > 0 && game.players.length === 4) {
      this.unlockAchievement('tripleKO');
    }
  }

  checkPlayAreaAchievements(playArea) {
    // Existing achievement checks...
    const nullCard = playArea.find(p => p.card.id === 'null' && p.playerId === 0);
    if (nullCard) {
      this.unlockAchievement('nullified');
    }

    const awarenessPlayed = playArea.some(p => 
      (p.card.id === 'awareness' || p.card.id === 'moonlight') && p.playerId === 0
    );
    if (awarenessPlayed) {
      this.unlockAchievement('awareness');
    }

    const saviorPlayed = playArea.find(p => p.card.id === 'savior' && p.playerId === 0);
    const assassinPlayed = playArea.find(p => p.card.id === 'assassin' && p.playerId === 0);
    if (saviorPlayed || assassinPlayed) {
      this.unlockAchievement('assassin');
    }

    const reflectionCard = playArea.find(p => 
      p.card.id === 'reflection' && 
      p.card.enhanced && 
      p.card.power === 9 && 
      p.playerId === 0
    );
    if (reflectionCard) {
      this.unlockAchievement('reflection');
    }

    // Friend pairs
    const penelope = playArea.find(p => p.card.id === 'penelope' && p.playerId === 0);
    const kc = playArea.find(p => p.card.id === 'kc' && p.playerId === 0);
    const feathers = playArea.find(p => p.card.id === 'feathers' && p.playerId === 0);
    const fevaa = playArea.find(p => p.card.id === 'fevaa' && p.playerId === 0);
    const binx = playArea.find(p => p.card.id === 'binx' && p.playerId === 0);
    const kite = playArea.find(p => p.card.id === 'kite' && p.playerId === 0);
    const subject192 = playArea.find(p => p.card.id === 'subject_192' && p.playerId === 0);
    const cyrus = playArea.find(p => p.card.id === 'cyrus' && p.playerId === 0);
    const ginger = playArea.find(p => p.card.id === 'ginger' && p.playerId === 0);
    const salvo = playArea.find(p => p.card.id === 'salvo' && p.playerId === 0);
    const chaos = playArea.find(p => p.card.id === 'chaos' && p.playerId === 0);
    const maturity = playArea.find(p => p.card.id === 'maturity' && p.playerId === 0);

    // Check pairs for achievements
    if (penelope && kc) {
      this.unlockAchievement('friendshipPower');
    }
    if (penelope && feathers) {
      this.unlockAchievement('mothersLove');
    }
    if (fevaa && binx) {
      this.unlockAchievement('demonAngel');
    }
    if (subject192 && cyrus) {
      this.unlockAchievement('subjectGuardian');
    }
    if (ginger && salvo) {
      this.unlockAchievement('gingerSalvo');
    }
    if (chaos && maturity) {
      this.unlockAchievement('perfectChaos');
    }
    if ((assassinPlayed || saviorPlayed) && kite) {
      this.unlockAchievement('thatComedian');
    }

    // Check for Glitch Kingdom achievement
    const hasNull = playArea.some(p => p.card.id === 'null' && p.playerId === 0);
    const hasMoonlight = playArea.some(p => p.card.id === 'moonlight' && p.playerId === 0);
    const hasAwareness = playArea.some(p => p.card.id === 'awareness' && p.playerId === 0);
    
    if (hasNull && (hasMoonlight || hasAwareness)) {
      this.unlockAchievement('glitchKingdom');
    }

    // Hollowgreen anomaly checks
    const hasHollowgreen = playArea.some(p => 
      ['blossom', 'herb', 'ciph', 'soiley', 'evergreen'].includes(p.card.id) && 
      p.playerId === 0
    );

    if (hasHollowgreen) {
      this.unlockAchievement('greenThumb');
    }

    // Check for awakened Ciph
    const ciphCard = playArea.find(p => 
      p.card.id === 'ciph' && 
      p.card.transformed && 
      p.playerId === 0
    );
    if (ciphCard) {
      this.unlockAchievement('awoken');
    }

    // Check for high shield value
    const highShieldCard = playArea.find(p => 
      ['blossom', 'herb', 'ciph', 'soiley', 'evergreen'].includes(p.card.id) && 
      p.card.shield >= 10 && 
      p.playerId === 0
    );
    if (highShieldCard) {
      this.unlockAchievement('shieldMaster');
    }

    // Check for Terra + Hollowgreen combo
    const terra = playArea.find(p => p.card.id === 'terra' && p.playerId === 0);
    if (terra && hasHollowgreen) {
      this.unlockAchievement('gardenParty');
    }

    // Check for Fire and Ice achievement
    const hasFrost = playArea.some(p => p.card.id === 'frost');
    const hasFireCard = playArea.some(p => ['ember', 'fester', 'salvo'].includes(p.card.id));
    if (hasFrost && hasFireCard) {
      this.unlockAchievement('fireAndIce');
    }

    // Check for Lavender + Hollowgreen achievement
    const hasLavender = playArea.some(p => p.card.id === 'lavender');
    if (hasLavender && hasHollowgreen) {
      this.unlockAchievement('plantSecrets');
    }

    // Check for Future Meets Past achievement
    const hasBlitz = playArea.some(p => p.card.id === 'blitz');
    const hasFevaa = playArea.some(p => p.card.id === 'fevaa');
    const hasBinx = playArea.some(p => p.card.id === 'binx');
    if (hasBlitz && hasFevaa && hasBinx) {
      this.unlockAchievement('futurePast');
    }

    // Check for Ringmaster achievement
    const ringmasterCard = playArea.find(p => p.card.id === 'ringmaster');
    if (ringmasterCard && Math.max(...playArea.map(p => p.card.power)) >= 20) {
      this.unlockAchievement('showmanship');
    }

    // Check for Sole achievement
    const soleCard = playArea.find(p => p.card.id === 'sole');
    const defeatedCards = playArea.filter(p => p.card.power <= 0).length;
    if (soleCard && defeatedCards >= 2) {
      this.unlockAchievement('lightInDarkness');
    }

    // Check for Leopold achievement
    const leopoldCard = playArea.find(p => p.card.id === 'leopold');
    const friendlyCardsReduced = playArea.filter(p => 
      ['penelope', 'kc', 'echo', 'feathers', 'fevaa'].includes(p.card.id) && 
      p.card.powerReduced
    ).length;
    if (leopoldCard && friendlyCardsReduced >= 3) {
      this.unlockAchievement('grumpyKindness');
    }

    // Check for Hitbox achievement
    const hitboxCard = playArea.find(p => p.card.id === 'hitbox');
    const hitboxWon = hitboxCard && 
      hitboxCard.card.power > 1 && 
      Math.max(...playArea.map(p => p.card.power)) === hitboxCard.card.power;
    if (hitboxWon) {
      this.unlockAchievement('hitboxHero');
    }
  }

  checkDeckAchievements(deck) {
    if (!deck) return;
    
    // Check for enhanced Moonlight
    const hasEnhancedMoonlight = deck.some(card => 
      card.id === 'moonlight' && card.enhanced
    );
    
    if (hasEnhancedMoonlight) {
      this.unlockAchievement('sisterDeleted');
    }

    // Check for all Hollowgreen anomalies
    const hollowgreenCount = deck.filter(card => 
      ['blossom', 'herb', 'ciph', 'soiley', 'evergreen'].includes(card.id)
    ).length;
  
    if (hollowgreenCount === 5) {
      this.unlockAchievement('parasite');
    }

    // Check for SuNex achievement
    if (deck.some(card => card.id === 'sunex')) {
      this.unlockAchievement('yourWorldIsMirror');
    }

    // Check for Sanctuary Squad achievement
    const squadMembers = ['subject_192', 'vicki', 'fossil'];
    if (squadMembers.every(id => deck.some(card => card.id === id))) {
      this.unlockAchievement('sanctuarySquad');
    }

    // Check for Goldie achievement
    if (deck.some(card => card.id === 'goldie')) {
      this.unlockAchievement('worldsRichest');
    }
  }
}